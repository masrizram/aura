# Context Documentation — Index

## Files
- `context-diagram.md` — full system boundary with every external interaction
  (user, repo FS, SQLite, git, tooling, LLM, config, checkpoint/memory/evidence files,
  LIMITATIONS.md input) and the explicit list of interactions verified absent.
